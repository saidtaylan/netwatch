apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "netwatch.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "netwatch.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- .Values.config | nindent 4 }}
